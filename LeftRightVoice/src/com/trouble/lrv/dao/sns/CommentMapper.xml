<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trouble.lrv.dao.sns.CommentMapper">
	<insert id="addPost" parameterType="Comment">
		insert into postcomment(postid, comtext, ownerid, insertDt)
			values(#{postId}, #{comText}, #{ownerId}, #{insertTime})
	</insert>
</mapper>